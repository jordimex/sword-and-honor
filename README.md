# Sword and Honor

Playable vertical slice scaffold for a Reddit Devvit RPG. The local build runs as a Vite app while the shared architecture is organized to plug into a Devvit server.

## Commands

- `npm install`
- `npm run dev`
- `npm run type-check`
- `npm run test`
- `npm run build`
